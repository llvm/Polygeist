# PolyBench Section 4.2 Reproduction Status

Last updated: 2026-09-08T14:15:00-07:00

## Paper-analysis generation (2026-09-08)

- BLOCKED ATTEMPT — GEMM publication-GPU closure first tried the cross-builder's minimal-runtime mode, but its retained runtime cleanup path still references cuDNN and cuSPARSE destructors while those DSOs are omitted. Linking failed before execution; no correctness or timing value was produced. The exact failed build is retained as `logs/publication_orin/gemm/raised-paper-correctness-build-minimal-rejected.log`, and the full external CUDA-library link is used next.
- REJECTED COMPARISON — The first comparison of the new ABI-only raised GEMM output used the old extracted `native-cpu-large.dump.log.gz`, which contains only 55,426 parsed values rather than the required 1,100,000. The new raised output contains all 1,100,000 values, but no correctness conclusion is taken from mismatched-length inputs; comparison is rerun against the retained complete native silicon log.
- PASS — The ABI-only raised GEMM executable was cross-built on x86, and its AArch64 relocation/disassembly proves `main → kernel_gemm → kernel_gemm_impl → polygeist_cublas_dgemm`; no original CPU kernel body is linked. On Orin it passes all 1,100,000 canonical LARGE/FP64 values against the complete fresh native-CPU silicon reference (`max_abs=0.01`, `max_rel=4.46388715e-05`, `rtol=5e-4`, `atol=1.1e-2`). Timing proceeds only after this gate.
- PASS — Fresh raised-GPU GEMM one-process timing on Orin completed with five warmups and five measured samples: median compute-device 71.664803 ms, synchronized compute-wall 71.652960 ms, compiler-visible memory-device 22.901729 ms, and end-to-end 94.744512 ms. Against fresh native PolyBenchGPU (74.829246521 ms device, 81.750000 ms end-to-end), raised GEMM is 1.044156x faster by the paper's device/device comparison and 1.158954x slower end-to-end. Raw logs and normalized samples are retained under `logs/publication_orin/gemm/`.
- PASS — Generated a publication-specific CPU ledger from the retained fresh Orin sample CSVs: 17 native-C/raised-OpenBLAS pairs, all with complete-output pass/pass correctness and exactly five warmups plus five samples. The provisional native/raised geometric-mean speedup is 7.698939x (median 9.302675x; range 1.268445x–32.906816x).
- PASS — Generated a publication-specific GPU ledger with 13 native and 17 raised measurements, including 12 one-to-one device-time pairs. The provisional geometric-mean native/raised device speedup is 1.504235x; raised is faster in 9/12 pairs. Device, synchronized resident-wall, compiler-visible memory-device, and end-to-end scopes remain separate.
- PASS — Generated reproducible SVG and PDF paper figures plus CSV, JSON, methodology, conversion-command, and raw-evidence links under `paper_analysis/`. The generator validates the five-warmup/five-sample contract rather than consuming the legacy one-warmup ledger.
- PASS — Added `polybench-paper.html` to the viewer landing page and global paper-analysis navigation. It presents the two figures, per-kernel medians/IQRs, correctness gates, device and end-to-end GPU scopes, modified-source status, raw evidence, related-work status, and explicit incomplete experiments. After publishing fresh GEMM on both PolyBench pages, the rebuilt viewer passes all 5,579 local links across 1,589 HTML pages. URLs: `file:///tmp/ir_viewer/polybench.html` and `file:///tmp/ir_viewer/polybench-paper.html`.
- PARTIAL — These figures are paper-layout-ready but remain publication-pending because the runner reported hardware/accelerator state as `N/A`; fixed power, clocks, fan, temperature, and throttling state have not yet been verified. KernelFaRer remains unavailable, and Polly is retained only as separately labelled x86 evidence.

## Strict recognition retraction (2026-09-07)

The raised-CPU, native PolyBenchGPU, and raised-GPU application paths below
used linker-symbol substitution: the original `kernel_*` definition was
weakened and a selected wrapper/adapter won at link time. These are manual
compositions, not automatic untouched-source transformations. Their builders
and weak-symbol paths are removed; all related correctness, runtime, speedup,
and PASS claims below are historical diagnostics and are **excluded from the
paper and current HTML totals**. Native optimized-C CPU results remain valid.

Reinstatement requires an in-place compiler transformation whose library
selection is based only on operations, dataflow, indexing, loop/control-flow
structure, types, and proven semantics. Renaming the function or moving the
source must not change recognition. No deleted, weakened, or interposed source
symbol may participate in the executable.

Reorganization update: 2026-09-05

Raised-GPU repair update: 2026-09-05

Native-GPU availability update: 2026-09-05

- PASS — Audited all 19 same-named CUDA sources in PolyBenchGPU commit `5584aaa7`. Thirteen are algorithmically comparable after protocol normalization and were cross-compiled on the x86 host; six are explicitly unavailable because their algorithms or live-out semantics differ from canonical PolyBench/C.
- PASS — Twelve of the thirteen comparable native-GPU configurations pass correctness and have one warmup plus five accepted samples: `2mm`, `3mm`, `atax`, `bicg`, `correlation`, `covariance`, `doitgen`, `fdtd-2d`, `gemm`, `gemver`, `gesummv`, and `mvt`.
- FAIL — Normalized PolyBenchGPU `gramschmidt` runs but fails FP64 output correctness (428,561 of 2,640,000 values); it has no accepted timing.
- UNAVAILABLE — PolyBenchGPU `adi`, `jacobi-1d`, `jacobi-2d`, `lu`, `syr2k`, and `syrk` are not equivalent canonical baselines. The remaining eleven kernels have no same-named external CUDA source in the audited repository.
- PASS — Regenerated the consolidated page from the shared main worktree with all 30 manifest rows, twelve accepted native-GPU timing pairs, nine accepted raised-GPU timing pairs, and overlapping passed/failed/unavailable/modified-source filters. The strict audit checks 8,034 local links across 1,634 HTML pages with zero broken targets. URL: `file:///tmp/ir_viewer/polybench.html`.

- FAIL — Current `f85ad6e4` baseline rerun reconfirmed `2mm`: the real cuBLAS wrapper executes, but all 960,000 output values are zero/wrong.
- FAIL — Current `f85ad6e4` baseline rerun reconfirmed `atax`: all 2,100 output values differ from the native reference.
- FAIL — Current `f85ad6e4` baseline rerun reconfirmed `bicg`: 2,098 of 4,000 values differ from the native reference.
- FAIL — Current `f85ad6e4` baseline rerun reconfirmed `gemm`: 1,098,823 of 1,100,000 values differ, beginning where later rows remain zero.
- FAIL — Current `f85ad6e4` baseline `gemver` now aborts with an illegal CUDA memory access rather than reaching comparison; no timing is accepted.
- PASS — Batched host-page registration fixes the shared stale-device-pointer bug for `atax` (exact) and `bicg` (within the declared tolerance) on the Jetson.
- PARTIAL — The same registration fix lets `gemver` complete without an illegal access, but its output remains numerically wrong. `2mm` and `gemm` remain all-zero/wrong, so their timings are still withheld.
- PASS — `gemm` now passes all 1,100,000 values on the Jetson. The device's CUDA 12.6 cuBLAS GEMM and GemmEx entry points independently return success while leaving output unchanged, so the runtime expresses GEMM as row-wise real-cuBLAS GEMV calls; no project computational kernel is introduced.
- PASS — `2mm` also passes after the real-cuBLAS GEMM-via-GEMV repair, including both dependent matrix products.
- PASS — `gemver` passes after marking the exposed original harness kernel `noipa`. Without that attribute, GCC optimized `main` using facts about the source body before its symbol was weakened, so the replacement wrapper's valid AArch64 caller-register clobbers changed the subsequent dump bound.
- PASS — `gesummv` and `mvt` now pass on the Jetson; batched registration removes their illegal accesses and the `noipa` harness protocol prevents source-body assumptions from crossing into the replacement wrapper.

- PASS — Rebased the isolated evaluation worktree onto shared `raisetolinalg` commit `e77f674c17d43f1a2bfbf62897e433d14ca74d5e`; Git skipped the already-applied Section 4.2 patch rather than duplicating it.
- PASS — Rebuilt `cgeist` and `polygeist-opt` from that exact clean evaluation worktree using `cmake --build /tmp/polygeist-section42-build --target cgeist polygeist-opt -j4`.
- PASS — Jetson control access has recovered: the device again reports Orin compute capability 8.7, driver 615.06, and CUDA compiler 12.6.85. Previous timed binaries were no longer present, so no old executable was reused.
- PASS — Sweep scripts now accept an optional explicit kernel list, allowing blocked and missing rows to be rerun without overwriting unrelated retained results.
- PASS — Reorganized the viewer around one manifest-driven PolyBench page with exactly four primary runtime columns: native CPU, raised CPU-library, native PolyBenchGPU CUDA, and raised CUDA-library. The old split `polybenchgpu.html` and `polybench-section42.html` outputs are removed, and the hardcoded legacy GPU timing table is no longer a data source.
- PASS — Added and passed a focused viewer regression for the four-column contract and obsolete-page cleanup.
- PASS — Current-branch OpenBLAS correctness revalidation reconfirmed `2mm`, `atax`, `bicg`, `gemm`, `gemver`, `gesummv`, and `mvt`; `doitgen` still fails ABI lowering before execution and `gramschmidt` fails numerically with non-finite output.
- FAIL — Recovered-device raised-GPU rerun of `3mm` completed but returned zero for all 880,000 output values; correctness failed and timing remains withheld.
- FAIL — Recovered-device raised-GPU rerun of `gesummv` aborted with an illegal CUDA memory access; correctness failed and timing remains withheld.
- FAIL — Recovered-device raised-GPU reruns resolved the remaining blocked rows: `gramschmidt` exceeded the device launch timeout and `mvt` aborted with an illegal memory access. No raised-GPU timing is accepted for either row.
- PASS — Cross-compiled canonical native-GPU adapters for `gemm`, `2mm`, `3mm`, `gesummv`, and `gemver` entirely on the x86 host. Each adapter retains the PolyBenchGPU commit `5584aaa7` computational kernel, uses canonical PolyBench/C LARGE dimensions, FP64 initialization and output, and is explicitly marked modified-source because integration and launch geometry were normalized.
- PASS — All five canonical PolyBenchGPU configurations pass the numeric criterion and have one warmup plus five accepted samples. Medians are recorded in `performance_gpu.csv` with separate CUDA-event device and PolyBench-call end-to-end scopes.
- PASS — Current-branch OpenBLAS correctness revalidation completed with the same seven passes (`2mm`, `atax`, `bicg`, `gemm`, `gemver`, `gesummv`, `mvt`) and the same two failures (`doitgen` lowering, `gramschmidt` numeric/non-finite output).
- PASS — Retimed all seven correctness-approved OpenBLAS rows on pinned CPU 21 with one BLAS thread, one warmup, and five samples. Current medians are 66.929 ms (`2mm`), 7.947 ms (`atax`), 7.219 ms (`bicg`), 50.437 ms (`gemm`), 69.042 ms (`gemver`), 3.416 ms (`gesummv`), and 7.934 ms (`mvt`).

## Scope and acceptance rules

- Source under evaluation: Polygeist commit `e77f674c17d43f1a2bfbf62897e433d14ca74d5e` (`raisetolinalg`), isolated in `/home/arjaiswal/Polygeist-section42` on branch `eval/polybench-section42`.
- The shared `/home/arjaiswal/Polygeist` checkout has unrelated uncommitted runtime, matcher, benchmark, and LLVM-submodule edits. They remain excluded from these measurements and are not modified by this work.
- Canonical suite: all 30 PolyBench/C kernels under `tools/cgeist/Test/polybench`, excluding the alternate `Nussinov.orig.c` source.
- A performance datum is accepted only after the same configuration passes output correctness against native C using identical source, dataset dimensions, datatype, and initialization.
- Only external CPU/GPU libraries are eligible for matched-path performance. Project-authored computational kernels and benchmark-specific matcher rules are prohibited.
- GPU device-only and end-to-end times are recorded separately.

## Concrete results and blockers

- PASS — Created isolated evaluation worktree and branch without touching the dirty source checkout.
- PASS — Enumerated 30 canonical PolyBench/C kernels.
- PASS — Host exposes OpenBLAS (`libopenblas.so`) and `aarch64-linux-gnu-gcc`.
- PASS — `pva-general` is reachable with non-interactive SSH.
- PASS — Direct Jetson access through `pva-general` is reachable: Orin (`sm_87`), driver 615.06, CUDA compiler 12.6.85, cuBLAS 12.6.4.1.
- PASS — Fixed the protocol at explicit `LARGE_DATASET`, `DATA_TYPE_IS_DOUBLE`, and the checked-in initialization for all configurations. Source SHA-256 values are recorded for all 30 rows.
- FIXED — The checked-in `run_all_e2e.sh` covered only 26/30 canonical kernels. The isolated branch now includes `adi`, `seidel-2d`, `durbin`, and `ludcmp`; the complete 30-row sweep ran.
- PASS — Clean `cgeist` and `polygeist-opt` build from the isolated worktree; configure/build logs and final binary SHA-256 values are retained in `logs/`.
- FIXED — Commit `c4dda7f5` loops indefinitely in the GEMM raising pipeline. Ported only the generic four-line invariant-hoisting guard from the dirty checkout; a new benchmark-name-independent dynamic GEMM regression completes in 0.01 seconds, and the formerly hanging PolyBench GEMM stage now completes in 0.01 seconds with four Linalg ops.
- BLOCKED — The checked-in matcher round-trip marker restoration produces duplicate `%extracted_slice` SSA definitions for GEMM, so that diagnostic-only output cannot be parsed by `mlir-opt`. Residual correctness is tested directly from raised/debufferized IR, and actual library paths are tested from semantic matcher output.
- PASS — `LARGE`/double exact native-vs-residual correctness: `gemm` (6.59 s total build/check), `2mm` (10.74 s), `3mm` (14.68 s), `atax` (0.91 s), and `bicg` (0.91 s).
- FAIL — `adi` still leaves `polygeist.submap`/`submapInverse` after raising and lower-submap, so it is rejected before CPU lowering (0.46 s). This reproduces a known failure; detailed IR diagnosis is pending.
- PASS — `cholesky` exact native-vs-residual correctness at `LARGE`/double (181 s total build/check).
- FAIL — `correlation` reaches execution but residual output differs from native, beginning with extra diagonal values (`2.00`); rejected after 57.90 s. This reproduces a known correctness failure.
- PASS — Rechecked known failure `covariance`: it now passes exact native-vs-residual correctness at `LARGE`/double (44 s). No performance value is accepted until matcher/library status is separately established.
- PASS — `deriche` (30 s), `doitgen` (15 s), and `fdtd-2d` (20 s) exact native-vs-residual correctness at `LARGE`/double.
- FAIL — `durbin` leaves two dynamic tensor `polygeist.submap` operations and is rejected before CPU lowering (0.41 s). This reproduces a known failure.
- PASS — `floyd-warshall` (102 s), `gemver` (2 s), `gesummv` (1 s), and `gramschmidt` (68 s) exact native-vs-residual correctness at `LARGE`/double.
- UNAVAILABLE — KernelFaRer commit `53a10e6` fails to build against both available LLVM 18 and LLVM 14 APIs. Preceding artifact commit `9d04406` also fails against LLVM 14 (`MatrixBuilder` API). No older compatible LLVM is installed; all three build logs are retained.
- PASS — System LLVM/Clang 14 exposes integrated Polly and accepts a Polly-enabled compilation probe. Common-subset correctness/timing remains pending.
- PASS — `heat-3d` (39 s), `jacobi-1d` (1 s), and `jacobi-2d` (12 s) exact native-vs-residual correctness at `LARGE`/double.
- PASS — Polly 14 exact correctness for common-subset `gemm` at `LARGE`/double.
- REJECTED — Initial GEMM native-Clang14/Polly timing batch overlapped the LU correctness sweep and showed strong contention variability. Those raw samples are retained but are not entered in `performance_cpu.csv`; timing will be repeated serially on an idle host.
- PASS — Completed the 30/30 `LARGE`/double native-vs-residual sweep. Exact comparison passes for 24 kernels. `correlation` is a numeric mismatch; `adi`, `durbin`, `ludcmp`, `nussinov`, and `seidel-2d` are partial lowering failures. All stage IR and error logs are retained.
- PASS — Late sweep results: `lu` (181 s), `mvt` (1 s), `symm` (16 s), `syr2k` (27 s), `syrk` (12 s), `trisolv` (1 s), and `trmm` (18 s).
- FAIL — `ludcmp` retains one dynamic tensor submap; `seidel-2d` retains eight tensor submaps; `nussinov` retains two memref submaps. Each is rejected before executable residual lowering.
- PASS — Matcher sweep completed for all 30 manifest rows. Twelve correctness-passing kernels emit external-library launches; twelve correctness-passing kernels have an explicit `no_match`; the six residual failures are matcher-unavailable. Full launch counts and kinds are in `logs/matcher_sweep_summary.csv`.
- FIXED — Multi-operation GEMM matching originally left later slice/insert users attached to the replaced scale result. The generic composition-root rewire now preserves those uses and has a benchmark-name-independent regression.
- FIXED — Enabling the CBLAS runtime exposed a C `<complex.h>` macro collision with two runtime parameter names. The generic rename is covered by a compile regression and the runtime now builds against system OpenBLAS.
- PASS — Matched GEMM executes through system OpenBLAS/CBLAS and passes the declared numeric criterion: 1,100,000 values, zero failures, maximum absolute error 0.01 and maximum relative error 4.45e-05 (`rtol=5e-4`, `atol=1.1e-2`). Exact comparison is inappropriate for reordered floating-point BLAS reductions; the comparator and close/far regression inputs are retained.
- PASS — Focused regressions for invariant hoisting, composition-root rewiring, and CBLAS runtime compilation pass. The numeric dump comparator also passes its close case and rejects its far case.
- PASS — OpenBLAS/CBLAS correctness passes for eight matched kernels: `2mm`, `atax`, `bicg`, `gemm`, `gemver`, `gesummv`, `gramschmidt`, and `mvt`. Each used the same source, `LARGE` dimensions, FP64 data, initialization, and retained native reference as the residual test.
- FAIL — Matched `doitgen` lowers two runtime calls but standard MLIR lowering rejects changed tensor loop-carried values after ABI lowering. It receives no CPU-library performance result unless repaired.
- UNAVAILABLE — `3mm` is matched only to cuTensorNet, while `covariance` and `deriche` emit only zero-fill launches; none is an OpenBLAS/CBLAS CPU-library configuration. Their CPU-library cells are not to be conflated with the eligible CBLAS sweep.
- PASS — Generic build-driver fixes now allow semantic-MLIR artifact export and initialize the selected stencil backend on the semantic-resume path.
- FAIL — First Jetson execution of matched GEMM builds and runs through real CUDA/cuBLAS but is numerically wrong: 1,098,823 of 1,100,000 values fail, beginning at index 1,100 with a zero result versus 453.66. No GPU timing is accepted. Cross-build, deployment, output, and comparison logs are retained.
- PASS — Full matched Jetson correctness sweep attempted all twelve launch-emitting kernels using host cross-compilation only. `atax`, `bicg`, `covariance`, `deriche`, `gemver`, `gesummv`, and `mvt` pass the numeric criterion through CUDA/cuBLAS or CUDA memset.
- PASS — `3mm` cross-builds against staged aarch64 cuTensorNet/cuTENSOR, deploys to the Jetson, and passes all 880,000 compared FP64 values exactly. The first attempt used an incomplete header root; its log is retained alongside the successful retry.
- FAIL — Jetson `2mm` executes but all 960,000 output values fail (the result is zero); GEMM fails 1,098,823/1,100,000 values; `gramschmidt` fails 428,561/2,640,000 values. No timing is accepted for these rows.
- FAIL — `doitgen` has the same loop-carried tensor ABI-lowering rejection on the GPU build as on CPU. All other correctness-passing residual kernels have no GPU library match and will be recorded explicitly as unavailable rather than timed.
- INVALIDATED — Audit of the first matched CPU/GPU sweep found that a trailing passthrough `-O3` overrode the build driver's intended harness `-O0`. GCC cloned the original weak kernel into `main`, allowing some executions to bypass the matched wrapper. All earlier matched pass/fail outcomes are retained as raw diagnostics but withdrawn; none is accepted. The generic driver now places `-O0 -fno-inline -fno-inline-functions` last, with a regression assertion, and every matched configuration will be rerun.
- PASS — Added a generic optional CUDA-event timing wrapper and focused generation regression. This reports device scope around the complete raised kernel; PolyBench's own timer independently reports end-to-end kernel-call time.
- PASS — Reran every OpenBLAS configuration after preventing harness cloning. The same eight kernels pass through the actual strong matched wrapper; `doitgen` remains an ABI-lowering failure. Six focused regressions, including the harness flag-order assertion, pass.
- PASS — Reran every matched Jetson configuration after preventing harness cloning. True passes are `3mm`, `atax`, `bicg`, `covariance`, `deriche`, `gemver`, `gesummv`, `gramschmidt`, and `mvt`; `3mm` again passes all 880,000 values using cuTensorNet/cuTENSOR.
- FAIL — True matched Jetson failures are `2mm` (960,000/960,000 output failures), `gemm` (1,098,823/1,100,000 failures), and `doitgen` (ABI-lowering rejection). The earlier `gramschmidt` mismatch was an invalidated harness-cloning run; the corrected run is exact.
- INVALIDATED — A second symbol audit found the canonical PolyBench kernels themselves are declared `static`. Without `-Dstatic=`, weakening cannot affect them and `main` still calls a local source kernel even at `-O0`. All matched outcomes stated above remain retained diagnostics but are again withdrawn. The corrected protocol exposes only the benchmark translation unit's kernel with `-Dstatic=`; `polybench.c` is compiled separately with that define filtered out. CPU and GPU matched sweeps are being rerun, and final acceptance additionally requires the linked binary to contain the strong wrapper and no local source-kernel implementation.
- PASS — Under the audited strong-wrapper path, OpenBLAS correctness passes for `2mm`, `atax`, `bicg`, `gemm`, `gemver`, `gesummv`, and `mvt`. `gramschmidt` is a genuine failure (1,598,673/2,640,000 values fail, including zeros, NaNs, and infinities), and `doitgen` remains a lowering failure. The comparator was strengthened to count and reject non-finite tokens, with a regression.
- PARTIAL — Under the audited strong-wrapper Jetson path, `covariance` and `deriche` pass exactly. `2mm`, `atax`, `bicg`, `gemm`, and `gemver` are genuine numeric failures; `doitgen` fails lowering. `gesummv` hung for more than two minutes and was terminated; afterward the Jetson CUDA driver reported the device busy/unavailable even with no compute process listed, so clean reruns of `3mm`, `gesummv`, `gramschmidt`, and `mvt` are externally blocked pending device recovery. No timing from this episode is accepted.
- REJECTED — The first pinned CPU timing attempt was stopped after four `cholesky` samples because its harness initialization was intentionally `-O0`; Cholesky's cubic initializer made each process spend roughly a minute outside the measured region. Those partial values remain raw only. The audited harness now uses `-O3` initialization while disabling inlining and semantic interposition of the exposed weak source kernel; object relocation checks prove `main` still resolves to the strong wrapper.
- REJECTED — Audit of the restarted CPU timing batch found native `deriche` reporting exactly zero: with `POLYBENCH_TIME` alone, Clang eliminated that pure source kernel because the output-print guard is normally false. The entire partial timing batch is retained but withdrawn for consistency. The final timing build defines both `POLYBENCH_TIME` and `POLYBENCH_DUMP_ARRAYS` for every configuration, discards dump text outside the timed region, and thus makes every live-out observably used.
- REJECTED — A second DCE audit found that Clang can still inline and delete native `deriche` even when dump output is enabled, because the resulting values are provably independent of its uninitialized input. Final native builds retain `-O3` inside a separately emitted kernel using `-fno-inline -fno-inline-functions`; every binary must pass a disassembly audit proving `main` calls its kernel. Timed binaries can then use `POLYBENCH_TIME` alone. Earlier DUMP-enabled residual samples remain eligible because their separately compiled wrapper/kernel objects are unchanged and dumping occurs after the timed region; earlier native samples are withdrawn.
- BLOCKED — A recovery probe at 15:43 PDT found no listed compute process but the previously passing covariance executable still aborts with CUDA error “CUDA-capable device(s) is/are busy or unavailable” (exit 134). No Jetson timing or blocked correctness rerun is fabricated; `logs/jetson_recovery_probe_1500.txt` is retained.
- SUPERSEDED — The initial audit correctly rejected unmodified PolyBenchGPU programs because they use independent FP32 sources, fixed dimensions, initialization, and timing scopes. After device recovery, five explicitly modified-source adapters normalized only their integration, dimensions, datatype, inputs, and timing while retaining the external computational kernels; their new results supersede the blanket unavailable status for those five rows.
- PASS — DCE-safe, pinned residual timing has five post-warmup samples for `2mm`, `3mm`, `atax`, `bicg`, `cholesky`, `covariance`, `deriche`, `doitgen`, `fdtd-2d`, `floyd-warshall`, `gemm`, `gemver`, and `gesummv`. The continuation is serial and still in progress; medians are not published until the batch and final native baseline complete.
- PASS — Additional five-sample residual timing completed for `gramschmidt` (70.84–73.99 s), `heat-3d`, `jacobi-1d`, `jacobi-2d`, and `mvt`.
- UNAVAILABLE — The `lu` residual timing warmup was killed by the host before producing a sample; at observation time swap was fully occupied (3.8 GiB) even though 57 GiB RAM was available. Its earlier correctness pass remains valid, but no LU performance value is accepted. The batch continued without inventing a result.
- PASS — Final native C timing completed for all 30 kernels: five post-warmup samples per kernel, Clang 18 `-O3`, CPU 21 pinning, and 30/30 direct-call disassembly audits. The corrected native `deriche` median is 80.851 ms rather than the rejected zero result.
- PASS — Accepted CPU timing ledger contains 64 rows: 30 native, 23 raised residual, seven correctness-approved OpenBLAS/CBLAS, and four correctness-approved Polly 14 rows. Every row has five samples and a median; `lu` is the sole correctness-passing residual without a timing because its warmup was killed by the host.
- PASS — Polly 14 exact output correctness passes for the complete common subset: `gemm`, `syr2k`, `2mm`, and `3mm`. Accepted medians and speedups versus the audited Clang 18 native baseline are in `performance_cpu.csv`.
- UNAVAILABLE — KernelFaRer is explicit for all four common-subset rows: both tested source revisions fail against the installed LLVM APIs, so no correctness or timing result is claimed.
- PASS — Jetson recovery resolved every formerly blocked raised-GPU row to an explicit correctness failure. No raised-GPU timing is accepted: ten eligible configurations fail correctness or lowering, while twenty are unavailable because raising fails or no eligible full computational-library match exists.
- PARTIAL — After generic atomic registration of overlapping mapped host operands and preventing whole-program IPA of the weak source kernel, audited raised-GPU correctness now passes for `2mm`, `atax`, `bicg`, `gemm`, `gemver`, `gesummv`, and `mvt`. `3mm` still returns an all-zero result through its cuTensorNet route; `gramschmidt` now completes instead of timing out but fails 1,598,673/2,640,000 values; `doitgen` still fails loop-carried tensor ABI lowering. No timing is accepted for the three remaining failures.
- PASS — A generic, layout-checked rank-2 `A[m,k] * B[k,n] -> C[m,n]` route now retains zero initialization and dispatches the contraction to real cuBLAS instead of cuTensorNet. Its benchmark-name-independent matcher regression passes, and audited Jetson `3mm` correctness passes all 880,000 FP64 values exactly. The remaining raised-GPU failures are `doitgen` and `gramschmidt`.
- PASS — `doitgen` now cross-builds and passes all 3,360,000 FP64 values. A benchmark-independent three-stage matcher recognizes zero-scratch + GEMV + identity-copy, redirects real cuBLAS GEMV with beta=0 to the final destination, snapshots overlapping GEMV inputs on device, and removes invariant tensor iter-args before bufferization. The prior ABI-lowering failure and the subsequent stale-copy overwrite are both retained in its logs. `gramschmidt` is the sole remaining raised-GPU correctness failure.
- PASS — A clean audited rerun confirms raised-GPU correctness for 11 configurations: `2mm`, `3mm`, `atax`, `bicg`, `covariance`, `deriche`, `doitgen`, `gemm`, `gemver`, `gesummv`, and `mvt`. Every binary was host cross-compiled, deployed through `pva-general`, symbol-audited, and compared against its retained native FP64 reference before timing.
- UNAVAILABLE — `gramschmidt` does not have a semantically valid full external-library GPU match. Its apparent GEMV operates on a dynamically masked triangular suffix and depends on loop-carried matrix state; replacing it with a full-vector cuBLAS call produced zeros/infinities, while an attempted exact sliced route exposed stale functional-tensor snapshots. The matcher now rejects masked GEMV instead of emitting an invalid library call. The no-match AArch64 residual diagnostic also fails and is not represented as GPU performance.
- PASS — Raised-GPU timing completed for the nine correctness-approved substantive cuBLAS paths with one discarded warmup and five recorded samples each. `performance_gpu.csv` now contains separate device and end-to-end medians for `2mm`, `3mm`, `atax`, `bicg`, `doitgen`, `gemm`, `gemver`, `gesummv`, and `mvt`; `covariance`/`deriche` zero-fill-only matches and `gramschmidt` are explicitly unavailable rather than misreported as GPU speedups.
- PASS — The same generic overwrite/alias fix makes `doitgen` pass through real OpenBLAS/CBLAS on the host. Five post-warmup, CPU-21-pinned samples give a 124.127 ms median (5.389× versus native C). The accepted CPU ledger now has 65 records, including eight correctness-approved CPU-library rows.
- PASS — Final raised-GPU manifest counts are nine pass and 21 unavailable, with zero failures and zero pending cells. The GPU timing ledger has 14 accepted records: five canonical PolyBenchGPU adapters and nine raised-GPU cuBLAS configurations.
- PASS — Four benchmark-independent regressions pass through the generated `lit.site.cfg.py`: rank-2 zero-init matmul routing, GEMV overwrite-through-scratch composition, invariant tensor iter-arg removal, and beta-zero cuBLAS launch lowering. The source-config invocation failure and successful 4/4 rerun are both retained in `logs/raised_gpu_fix_regressions.log`.
- PASS — Rebuilt the consolidated four-runtime viewer after the repairs. It contains all 30 manifest kernels, displays the nine accepted raised-GPU timing pairs, provides nonempty passed/failed/unavailable/modified-source filters, and removes the obsolete split PolyBench pages. The final strict audit checks 7,894 local links across 1,634 HTML pages with zero broken targets. URL: `file:///tmp/ir_viewer/polybench.html`.
- SUPERSEDED — The earlier explicit-status audit recorded raised CPU library 7 pass/2 fail and raised GPU 10 fail/20 unavailable. The repaired, revalidated final counts below replace those values.
- FIXED — The first full four-runtime viewer attempt used the Slack virtual-environment Python, which lacks `egglog`, and was rejected. A retry with `/usr/bin/python3` rendered all kernel families but correctly stopped on a stale Section 4.2 artifact link left by the other worktree. The generated-output directory was preserved as `/tmp/ir_viewer_pre_four_runtime_20260905` before the final clean rebuild.
- PASS — Rebuilt the complete viewer from an empty output directory with `/usr/bin/python3`, verified the single PolyBench landing/navigation entry, exactly four runtime headers, all 30 manifest rows, and absence of the obsolete split pages. The strict link audit checked 7,822 local links across 1,633 HTML pages with zero broken targets. Result URL: `file:///tmp/ir_viewer/polybench.html`.
- PASS — Added opt-in, benchmark-independent GPU-region instrumentation after residency/inlining and before CUDA-graph wrapping. It uses one compute envelope per top-level dispatch span, temporarily classifies compiler-visible `gpu.alloc`, `gpu.dealloc`, and `gpu.memcpy` operations wherever they occur, records CUDA events without per-dispatch synchronization, and synchronizes once at region exit. It reports compute-device, memory-device, per-category wall/device, and end-to-end wall times. The existing `polygeist_build.sh` is unchanged.
- PASS — The focused compiler regression passes through the generated lit configuration. Both runtime implementations compile; the CUDA implementation was cross-compiled on x86 with GCC 11.4 against CUDA 12.6 SBSA headers.
- PASS — Jetson silicon validation is correctness-clean for resident FP64 `gemm` and the repeated-dispatch `doitgen`. Final single-run diagnostics are GEMM: 244.259 ms compute-device, 23.874 ms compiler-visible memory-device, 268.838 ms end-to-end; Doitgen: 1388.434 ms compute-device, 17.287 ms compiler-visible memory-device, 1405.876 ms end-to-end. Both use 28 event markers total; Doitgen does not create 1,200 per-call timing scopes. Numeric comparison passes 1,100,000/1,100,000 and 3,360,000/3,360,000 values respectively. Commands, hashes, raw records, and the rejected adapter-interception experiment are retained in `logs/gpu_region_timing_validation.log`.
- PASS — The full correctness-gated GPU-region retiming sweep is in progress. `2mm` completed with five accepted post-warmup samples: median compute-device 290.617 ms, compiler-visible memory-device 46.757 ms, and end-to-end 336.142 ms. Correctness passes all 960,000 FP64 values; raw samples and build/deployment commands are retained under `logs/2mm/raised_gpu_region_*`.
- PASS — GPU-region retiming completed for `3mm`: median compute-device 476.892 ms, memory-device 73.840 ms, and end-to-end 553.740 ms; correctness passed before timing.
- PASS — GPU-region retiming completed for `atax`: median compute-device 51.219 ms, memory-device 19.471 ms, and end-to-end 70.677 ms; correctness passed before timing.
- PASS — GPU-region retiming completed for `bicg`: median compute-device 47.500 ms, memory-device 21.194 ms, and end-to-end 69.377 ms; correctness passed before timing.
- PASS — GPU-region retiming completed for `doitgen`: median compute-device 1421.120 ms, memory-device 16.453 ms, and end-to-end 1438.330 ms; correctness passed all 3,360,000 FP64 values before timing and every run used only 28 markers around the repeated dispatch loop.
- PASS — GPU-region retiming completed for `gemm`: median compute-device 240.481 ms, memory-device 26.235 ms, and end-to-end 267.094 ms; correctness passed before timing.
- PASS — GPU-region retiming completed for `gemver`: median compute-device 119.012 ms, memory-device 20.835 ms, and end-to-end 139.873 ms; correctness passed before timing.
- PASS — GPU-region retiming completed for `gesummv`: median compute-device 42.194 ms, memory-device 19.545 ms, and end-to-end 62.273 ms; correctness passed before timing.
- PASS — GPU-region retiming completed for `mvt`: median compute-device 48.668 ms, memory-device 21.030 ms, and end-to-end 69.993 ms; correctness passed before timing.
- PASS — GPU-region retiming completed for `syr2k` through real `cublasDsyr2k`: median compute-device 226.521 ms, memory-device 24.960 ms, and end-to-end 253.400 ms; correctness passed before timing.
- PASS — GPU-region retiming completed for `syrk` through real `cublasDsyrk`: median compute-device 145.095 ms, memory-device 18.271 ms, and end-to-end 163.362 ms; correctness passed all 1,440,000 FP64 values exactly before timing.
- PASS — The raised-GPU region retiming sweep is complete for all 11 currently supported external-library configurations. Every row cross-built on x86, deployed to the Jetson, passed correctness, discarded one warmup, and retained five accepted samples. There are no failed or pending rows in this sweep; unsupported manifest kernels remain explicitly unavailable for the previously recorded raising/matching reasons.
- PASS — Published all 11 fresh medians to `performance_gpu.csv`, added `syrk`/`syr2k` as explicit raised-GPU passes in the 30-row manifest, and retained a compact compute/memory/E2E summary plus every raw sample. The focused instrumentation regression and external-CBLAS SYRK/SYR2K runtime regression pass; `git diff --check` is clean.
- BLOCKED — The first post-update viewer link audit raced with an unrelated concurrent process regenerating the shared `/tmp/ir_viewer`, so a generated ATen page disappeared during traversal. That audit is rejected rather than reported as a pass; the viewer will be refreshed and audited again after the other writer exits.
- PASS — The regenerated PolyBench page displays all 11 fresh raised-GPU timing pairs and links them to the new region-timing raw CSVs. A page-scoped audit checked all 241 links from `polybench.html` with zero broken targets. The unrelated full-site writer is still active, so only the page-scoped audit—not a stable whole-site audit—is claimed here.
- FIXED — The consolidated main worktree had omitted the already accepted native PolyBenchGPU `syrk` and `syr2k` rows that remained in the dedicated SYRK/SYR2K worktree. Restored their correctness logs, six raw runs apiece, manifest pass/modified-source statuses, and medians: `syrk` 364.816 ms device / 436.128 ms E2E; `syr2k` 893.415 ms device / 969.602 ms E2E. The refreshed 30-row page has 245 links and zero broken targets.
- FIXED — GPU speedups in the viewer now prefer correctness-approved device/device medians; E2E remains visible only as a diagnostic and is used only as a fallback when device measurements do not exist. This changes the displayed comparisons for `doitgen` to 0.05× device (77.349 / 1421.120 ms) and `gemver` to 0.08× device (9.541 / 119.012 ms).
- EXPLAINED — Raised `doitgen` preserves the canonical loop structure as 150×140 = 21,000 separate 160×160 FP64 cuBLAS GEMV calls, each followed by the generated pipeline boundary, whereas native CUDA uses 150 fused compute kernels plus 150 copy kernels. Raised `gemver` decomposes three fused native kernels into two DGER calls, two DGEMVs, Dscal+Daxpy, and three materialization kernels; the two DGERs also make two full matrix passes instead of the native rank-2 update's single fused pass. These launch, synchronization, and extra-memory-traffic costs explain the device-time regressions.
- PASS — Added benchmark-independent control-flow pipeline-scope coalescing. It lifts balanced per-dispatch begin/end calls across affine/SCF control flow only when every intervening operation is a CUDA dispatch or host-side shape/view/pointer metadata. Host loads, stores, copies, unknown calls, and computation remain hard barriers. Positive nested-loop and negative host-store regressions pass, as do all pre-existing wrapping and CUDA Graph checks.
- PASS — Fresh host-cross-compiled Jetson binaries for `doitgen` and `gemver` each pass their unchanged LARGE/FP64 native reference (`doitgen`: 3,360,000 values; `gemver`: 2,000 values) before timing. With one discarded warmup and five accepted runs, `doitgen` improves from 1421.120 ms to 456.762 ms compute-device (3.111× faster; raised/native GPU ratio 0.169×) and from 1438.330 ms to 474.301 ms E2E. `gemver` improves from 119.012 ms to 77.329 ms compute-device (1.539× faster; raised/native GPU ratio 0.123×) and from 139.873 ms to 98.273 ms E2E. Exact commands, hashes, raw six-run records, build logs, correctness reports, and coalesced IR are retained.
- REJECTED — A follow-up compiler-proven device-pointer scope also passed correctness but produced no measurable improvement. Runtime profiling showed `gemver`'s approximately 70 ms first-DGEMV submission delay remained after bypassing pointer classification, locating it inside lazy cuBLAS GEMV setup/submission after the preceding device work. That unhelpful implementation was removed; contraction batching/fusion remains the next optimization rather than publishing unsupported results.
- PASS — Rebuilt `3mm`, `atax`, `bicg`, `gesummv`, and `mvt` with generic control-flow scope coalescing using only the x86 cross toolchain; each final IR contains one pipeline begin/end pair. Fresh Jetson correctness passes against the unchanged LARGE/FP64 native references: `3mm` 880,000/880,000 values, `atax` 2,100/2,100, `bicg` 4,000/4,000, `gesummv` 1,300/1,300, and `mvt` 4,000/4,000. Timing is in progress and no ledger value has yet been replaced.
- PASS — Coalesced-scope retiming completed with one discarded warmup and five accepted Jetson runs per kernel. New compute-device/E2E medians are `3mm` 477.489/554.605 ms, `atax` 47.704/71.078 ms, `bicg` 48.369/71.331 ms, `gesummv` 41.559/64.135 ms, and `mvt` 47.590/70.246 ms. Versus the prior device medians, the changes are +0.1%, -6.9%, +1.8%, -1.5%, and -2.2%, respectively. This confirms synchronization coalescing materially helps repeated-loop `doitgen`, but does not remove the short-kernel cuBLAS startup/decomposition bottleneck. The accepted ledger, exact commands/hashes, build logs, correctness reports, all raw samples, and final coalesced IR are updated.
- CORRECTED — Treat normalized PolyBenchGPU `syrk` and `syr2k` as comparable because both native and raised implementations pass the benchmark's required canonical lower-triangle output contract. The fact that PolyBenchGPU additionally computes the upper triangle is retained as an implementation note, not an exclusion. Device/device speedups are now `syrk` 2.51× and `syr2k` 3.94×; the obsolete “not comparable” text has been removed. The refreshed 30-row page again passes its 245-link audit with zero broken targets.
- PASS — Independently rebuilt native `2mm` on the x86_64 Cascadelake host with Clang 18 `-O3 -fno-inline -fno-inline-functions`, LARGE/FP64, audited the retained direct `main -> kernel_2mm` call, pinned to the declared CPU 21, discarded one warmup, and recorded five new samples. The fresh 2764.417 ms median is 0.575% below the published 2780.394 ms median, confirming the existing ledger value. A CPU-20 diagnostic was slower/noisier and is rejected because it is not the declared pinned-core protocol. Commands, binary hash, samples, and host-load observation are retained in `logs/2mm/native_x86_recheck_20260907.log`.
- PASS — Added generic program-level uniform-zero propagation between matched candidates and ordinary BLAS ABI lowering. Zero state is proved through tensor casts, slices, insert-slices whose source and destination are both zero, and Polygeist submap views; ordinary FP32/FP64 GEMV and GEMM calls receive a normalized `polygeist.blas_beta_zero` fact and lower with `beta=0`, without requiring another `_zero` matcher symbol. Dynamic GEMM beta values are conservatively rejected because NaN times zero is observable. A whole-value fill is erased only when it is the exclusive accumulator use; slice-scoped fills remain until complete overwrite coverage can be proved. The focused positive/negative suite and 59 related launch-lowering tests pass. The complete 244-test tree reports 222 passes, two expected failures, 14 pre-existing no-RUN unresolved inputs, and six unrelated existing failures; exact diagnostics are retained rather than claimed as a clean full suite.
- PASS — Cross-built fresh Jetson binaries for `3mm`, `atax`, `bicg`, `gesummv`, and the negative-control `mvt` entirely on the x86 host. All five pass their unchanged LARGE/FP64 references (`3mm` 880,000 values, `atax` 2,100, `bicg` 4,000, `gesummv` 1,300, `mvt` 4,000). The generated ABI has `beta=0` on all three `3mm` GEMMs and both GEMVs in each zero-initialized vector kernel; `mvt` correctly remains `beta=1`. Alternating old/new binaries on the same device isolates a modest `3mm` compute-device improvement, 199.092 to 195.273 ms (1.020×), while `atax`, `bicg`, and `gesummv` are unchanged within run noise because their dynamically sliced fills cannot yet be erased safely. Much larger absolute changes seen in both new and control binaries during the session are treated as device/cache state, not credited to this compiler change, and the accepted performance ledger is therefore not replaced.
- PASS — Replaced the matcher-name-based zero special case with a reusable, target-independent uniform-constant analysis. It recognizes typed constants from semantic `linalg.fill` and complete identity `linalg.generic` producers, propagates them through casts, slices, agreeing insert-slices, Polygeist submaps, agreeing `scf.if` results, and unchanged `scf.for` iter-args, then materializes proven scalar library operands before the shared CPU/GPU ABI lowering. Arbitrary nonzero constants and integer constants are covered by focused tests; partial/diagonal writes are rejected.
- PASS — Added conservative overwrite-coverage handling for complete identity extract/insert slice shells. Initializers are removed only when the overwrite is statically full or dynamic slice extents are directly equal to the destination tensor dimensions. The remaining PolyBench dynamic-bound cases deliberately retain their fills because the current IR does not prove those scalar bounds equal to descriptor dimensions.
- PASS — The focused and related launch-lowering regression set passes 60/60. The complete 245-test tree reports 223 passes, two expected failures, 14 no-RUN unresolved fixtures, and the same six unrelated existing failures; this change introduces no new failure.
- PASS — Real OpenBLAS/CBLAS correctness passes with identical LARGE/FP64 sources and inputs for `3mm`, `atax`, `bicg`, `gesummv`, `gemver`, and `doitgen`. `syrk` and `syr2k` pass as residual controls but their retained matched artifacts emit no CBLAS shim call, so no CPU-library claim is made for those two. The newly enabled `3mm` CBLAS path has five post-warmup CPU-21-pinned, single-thread samples with a 127.482 ms median, 43.348× faster than its accepted 5526.133 ms native baseline.
- PASS — Fresh x86-cross-compiled Jetson builds using the generalized pass pass correctness for `3mm`, `atax`, `bicg`, and `gesummv`. Their current-session medians are retained as diagnostics (`3mm` 195.089 ms device / 248.685 ms E2E; `atax` 13.812 / 41.362 ms; `bicg` 13.398 / 40.640 ms; `gesummv` 5.393 / 29.678 ms), but are not substituted into the accepted GPU ledger because the control binaries experienced the same large session-wide device/cache shift. The compiler delta itself does not erase their dynamically bounded fills.
- PARTIAL — Ordinary GEMM/GEMV no longer requires a constant-specific matcher name to consume a proven accumulator constant. Compatibility symbols remain for intrinsically overwrite-style compositions such as `cublasDgemv_T_zero`; replacing those symbols needs a separate generic destination-read/overwrite semantic, not further constant propagation.
- PASS — Audited every remaining raising and computational-library gap against the canonical source, retained raised IR, installed OpenBLAS symbols, CUDA 12.6 SBSA headers, and current external-library documentation. Direct near-term matches are Trisolv→DTRSV, SYMM→DSYMM, TRMM→DTRMM, CPU SYRK/SYR2K, and Cholesky→DPOTRF. Exact multi-call candidates exist for ADI, covariance, correlation, Durbin, Gram-Schmidt, and Seidel; stencil, FDTD, Floyd-Warshall, Deriche, and no-pivot LU routes require additional external-library infrastructure. Nussinov has no credible whole-operation external-library match. The full assessment is in `LIBRARY_MATCH_GAP_AUDIT.md`.
- PASS — The existing generic whole-recurrence Trisolv route cross-builds at the required LARGE/FP64 configuration into exactly one real cuBLAS DTRSV call and passes Jetson correctness at `n=2000` with maximum relative error `4.506e-12`. Its manifest entry remains unchanged until the result is integrated into the canonical sweep and timed.
- FAIL — The existing Cholesky route cross-builds at LARGE/FP64 into exactly one real cuSOLVER DPOTRF call, but the Jetson result fails the strict canonical source-output comparison at `n=2000` (`max_abs=max_rel=654.865`). The validation harness was corrected to reproduce PolyBench/C's exact `t,r,s` initializer order and the host-cross-compiled binary was rerun; the same failure remains. Earlier MINI/MEDIUM passes do not override the required-size failure; no timing is accepted.

## Required outputs

- `manifest.csv`: final 30-row explicit per-stage ledger; zero pending cells.
- `performance_cpu.csv`: 65 accepted correctness-gated median timings with scope, library, command, and raw-log paths, including eight raised OpenBLAS/CBLAS rows.
- `performance_gpu.csv`: correctness-gated native and raised GPU records, including the 11 freshly remeasured raised CUDA-library configurations. Raised rows report compute-only device time and end-to-end time separately; compiler-visible memory-device medians and all raw samples are retained in their logs.
- `logs/`: retained commands, versions, builds, raw samples, rejected attempts, comparisons, symbol audits, failure reasons, viewer build, and viewer link check.

## Incomplete experiments

The run is closed with explicit outcomes rather than fabricated substitutes. Native GPU was attempted for every algorithmically comparable CUDA source in the audited PolyBenchGPU revision: twelve pass and one (`gramschmidt`) fails correctness. Seventeen rows are unavailable because the external repository either has no corresponding CUDA source (eleven) or its same-named source computes a materially different algorithm/result (six). Remaining incomplete experiments are KernelFaRer on a compatible LLVM toolchain, residual timing for host-killed `lu`, five submap-lowering failures, and the `correlation` residual numeric failure. The viewer repeats the failure reason for every affected kernel.
- CORRECTION — CPU-library support for `syrk` and `syr2k` is not a new-library gap. OpenBLAS-backed `cblas_dsyrk`/`cblas_dsyr2k` runtime shims already exist in `raisetolinalg`, and the complete matcher/lowering plus passing CPU measurements were committed on `fix/polybench-syrk-syr2k` as `a6c10e2a`. The current branch is missing that commit's canonical launch dispatch and retained CPU result rows, so this is a branch-integration gap.
- PASS — Ported canonical `syrk`/`syr2k` matcher and ABI lowering from `a6c10e2a` into the current `raisetolinalg` code. Focused matcher checks emit exactly one `@cublasDsyrk`/`@cublasDsyr2k` launch with no residual `linalg.generic`; focused lowering emits the corresponding OpenBLAS runtime shim calls; the runtime unit test passes.
- PASS — Revalidated LARGE FP64 CPU-library correctness on the current branch: `syrk` and `syr2k` both build with one real OpenBLAS call and pass comparison against their canonical native outputs. `syrk` is bit-identical; `syr2k` passes the established `rtol=5e-4`, `atol=1.1e-2` tolerance.
- FIXED — Host correctness builds now omit GCC-only `-fno-ipa-*` options when the selected harness compiler is Clang; this removes a generic build failure without weakening the existing no-inline/interposition guard.
- PASS — Integrated tensor-carried `trisolv` whole-recurrence recognition into the canonical Section 4.2 matcher sweep. LARGE FP64 passes exactly through real OpenBLAS `cblas_dtrsv` on CPU and real cuBLAS `cublasDtrsv` on the Jetson. Fresh medians: CPU-library 2.294 ms; raised GPU device 9.279 ms and end-to-end 111.280 ms. The GPU timing was accepted only after correctness passed.
- FIXED — Minimal-cuBLAS cross-links now suppress the unrelated cuDNN stencil teardown reference, restoring lean Jetson builds without adding cuDNN to a DTRSV-only executable.
- PASS — Added structural whole-operation recognition and real external-library lowering for row-major left/lower FP64 `symm`. LARGE correctness passes through OpenBLAS `cblas_dsymm` and, on the Jetson, an equivalent sequence of real cuBLAS `cublasDsymv` calls. Direct `cublasDsymm` returned success but left the Jetson output unchanged, so it was rejected. Accepted fresh medians: CPU-library 48.761 ms; raised GPU device 112.214 ms and end-to-end 211.362 ms.
- PASS — Added structural whole-operation recognition and real external-library lowering for row-major left/lower/transposed/unit-diagonal FP64 `trmm`. LARGE correctness passes through OpenBLAS `cblas_dtrmm` and an equivalent cuBLAS `cublasDtrmv` + `cublasDscal` column composition. Direct Jetson `cublasDtrmm` produced zero output and was rejected. Accepted fresh medians: CPU-library 31.027 ms; raised GPU device 305.145 ms and end-to-end 394.860 ms.
- PASS — Focused regression suite for symmetric rank-k, tensor-carried TRISOLV, whole SYMM/TRMM recognition, ABI lowering, and external CPU runtime completed: 9 passed, 0 failed. Result finalization retains 30 explicit manifest rows and 70 CPU records; the viewer link audit passes 5,428 local links across 1,587 pages.
- PASS — Final PolyBench results page regenerated at `/tmp/ir_viewer/polybench.html` after the stable finalizer update. It shows the new CPU-library and raised-GPU measurements for SYRK, SYR2K, TRISOLV, SYMM, and TRMM; all 30 manifest rows have explicit statuses. Final local-link audit: 5,428 links across 1,587 HTML pages, 0 broken.
- IN PROGRESS — Reopened the remaining Section 4.2 gaps. Applying the current `--lower-polygeist-submap` pass to the retained debufferized IR eliminates every residual view in `durbin`, `ludcmp`, and `seidel-2d`; `adi` retains four `submapInverse` write-backs and `nussinov` retains two memref input/output views. A fresh canonical LARGE `durbin` build reached `--linalg-debufferize` but remained at 100% CPU for 9 minutes 9 seconds and was interrupted; no correctness or timing claim is made. This identifies a pipeline-order/compile-time blocker rather than the previously reported unsupported-view count.
- FIXED — Added generic lowering for `memref.load`/`memref.store` through arbitrary affine `polygeist.submap` views. The residual harness now uses this after converting already-raised Linalg to explicit loops when reversed or runtime-symbol views cannot become `memref.subview`; ordinary matchable kernels retain the tensor-debufferized path. The focused regression passes, all five previously blocked kernels emit zero residual Polygeist operations, and the former Durbin compile-time stall is removed.
- PASS — Fresh canonical LARGE/FP64 residual correctness passes exactly for `durbin`, `nussinov`, and `seidel-2d`. End-to-end build-and-compare wall times were 0.88 s, 173.58 s, and 68.10 s respectively. MINI also passes for all three. These are correctness results, not accepted performance timings.
- FAIL — `adi` and `ludcmp` now lower, compile, and execute at MINI, but differ from their native outputs. They are no longer classified as raising failures; correctness must be fixed before timing or library matching. ADI loses the expected all-one boundary/output values, while Ludcmp's solved vector starts near 4–5 instead of the native 4 followed by near-zero values.
- FIXED — The initial ADI/Ludcmp mismatch was caused by unrestricted greedy folding after loop conversion: dynamically offset `memref.subview` accesses were rewritten with MLIR's `INT64_MIN` dynamic sentinel as a literal index. Post-loop submap cleanup is now restricted to direct submap load/store/dim users, and the fallback deliberately avoids a subsequent broad canonicalizer. The regression covers reversed input access, symbolic output access, and dynamic view dimensions.
- PASS — All five former raising failures now pass canonical LARGE/FP64 residual correctness exactly: `adi` (45.05 s complete build-and-compare), `durbin` (0.88 s), `ludcmp` (265.91 s), `nussinov` (173.58 s), and `seidel-2d` (68.10 s). These wall times are diagnostic end-to-end validation durations, not accepted kernel performance measurements.
- FIXED — Correlation's triangular `j=i+1..M` wrapper around the nested dot-product lost its lower-bound predicate because the raiser gated only direct-store yields, not values yielded by a nested `linalg.generic`. Bound masking now tracks the old destination block argument for every yielded result. The raised contraction contains `j >= i+1` gating, and the focused regression passes.
- PASS — Canonical Correlation residual correctness now passes exactly at MINI and LARGE/FP64. The LARGE complete build-and-compare took 62.89 s; this is a validation duration, not an accepted kernel timing. The prior residual correctness failure is closed.
- CORRECTED — A fresh current-tree Gram-Schmidt matcher audit finds five Linalg bodies and emits zero external-library launches. The only computational candidate is a transposed GEMV over a dynamically masked triangular suffix, and the legality check rejects the whole-vector substitution (`masked_gemv_reject`). The old OpenBLAS and raised-GPU failures came from that invalid substitution and must be reported as unavailable/no-match, not as failures of a valid external-library path. Native PolyBenchGPU remains a separate numerical failure.
- PASS — Cholesky now passes the canonical LARGE/FP64 contract through real OpenBLAS `DPOTRF` on x86 (`max_abs=1.5121e-13`) and through a real cuSOLVER/cuBLAS blocked composition on Jetson (`max_abs=1.9218e-13`). Legacy and generic whole-matrix cuSOLVER both returned `info=0` but left the same late matrix region unfactored; explicit staging did not change it. A 512-column blocked factorization isolated two additional Jetson library defects: `DTRSM` and submatrix `DSYRK` returned success without the required updates. Replacing those two calls with equivalent real-library `DTRSV` and `DGEMV` compositions passes all lower-triangle values. No project-authored computational kernel is used.
- PASS — LU's missing residual timing is closed with the generic opt-in loop-lowered residual route. The former functional-tensor binary reached 59,887,328 KB RSS and was killed after 208.16 s; the allocation-free raised route passes exact LARGE/FP64 correctness at 105,280 KB. With CPU 21 pinning, one discarded warmup, and five accepted samples, its median kernel time is 17,547.201 ms (0.669× the 11,734.263 ms native baseline). Canonical initialization remains outside the measured PolyBench kernel region.
- FAIL — Gram-Schmidt now has a semantically complete, benchmark-name-independent whole-algorithm match: DNRM2, DCOPY/DSCAL, shrinking GEMV, and GER implement each modified Gram-Schmidt step with real OpenBLAS/cuBLAS calls and the loop-carried A/R/Q state. This fixes the former invalid whole-vector GEMV substitution, but LARGE numerical correctness still fails because the canonical ill-conditioned recurrence is highly sensitive to BLAS reduction order. OpenBLAS fails 492,160/2,640,000 printed values (first `R[503][504]`, native -1.18 vs -0.07); cuBLAS fails 490,516 values (same location, -5.62). No timing is accepted. Native PolyBenchGPU's independent numerical failure remains.
- PASS — Correctness-gated Cholesky timing is complete. OpenBLAS `DPOTRF` has a 64.021 ms CPU-21-pinned median (29.975× versus native C's 1,919.037 ms). The blocked cuSOLVER/cuBLAS Jetson composition has a 543.584 ms device median and 632.020 ms end-to-end median. Each result discards one warmup and uses five accepted samples; the GPU ledger keeps device and end-to-end scopes separate.
- PASS — Added a name-independent whole-operation covariance match that proves the mean reduction, centering, Gram product, sample-count normalization, and symmetric result before dispatch. Real OpenBLAS (`DGEMV` + `DGER` + `DGEMM`) and real cuBLAS (`DGEMV` + `DGER` + column `DGEMV`) both pass all 1,440,000 canonical LARGE/FP64 values. Correctness-gated medians are 82.939 ms on pinned x86 CPU 21 and 590.682 ms device / 659.460 ms end-to-end on Jetson; one warmup is discarded and five samples retained.
- PASS — Added a name-independent whole-operation correlation match that proves mean, standard deviation with epsilon handling, standardization, triangular Gram calculation, symmetry, and unit diagonal. The external OpenBLAS and cuBLAS compositions both pass all 1,440,000 canonical LARGE/FP64 values exactly. Correctness-gated medians are 94.717 ms on pinned x86 CPU 21 and 774.067 ms device / 843.458 ms end-to-end on Jetson. This is separate from and in addition to the repaired residual CPU path and the normalized native PolyBenchGPU pass.
- UNAVAILABLE — KernelFaRer was rechecked against the host inventory. LLVM 14 is still the only installed packaged LLVM tree; both retained KernelFaRer revisions fail its `MatrixBuilder`/`llvm::Value` API at compile time, and no compatible older LLVM is installed. The current inventory and both exact failure tails are retained in `logs/kernelfarer_toolchain_recheck_20260907.log`; no result is fabricated.
- PASS — Regenerated the consolidated four-runtime results page after finalizing 30 explicit manifest rows and 74 accepted CPU records. Covariance and correlation display their newly correctness-gated OpenBLAS, native PolyBenchGPU, and raised cuBLAS measurements, with GPU device and end-to-end scopes separate. The full local audit passes 5,440 links across 1,587 HTML pages with zero broken targets. URL: `file:///tmp/ir_viewer/polybench.html`.
- IN PROGRESS (2026-09-08) — Reopened measurement as a publication-protocol Orin campaign under `agents.md` and `PUBLICATION_BENCHMARK_METHODOLOGY.md`. Earlier x86 CPU timings, one-warmup GPU sweeps, and any linker-substitution experiments remain diagnostic and will not be reused as paper results. The required protocol is one selected Jetson AGX Orin, x86-host cross-compilation only, identical LARGE/FP64 sources and inputs, complete-output correctness before timing, five untimed warmups plus five synchronized samples in one process, pinned single-core/single-thread CPU runs, and separate resident device and end-to-end GPU scopes.
- PASS (2026-09-08) — The publication path now builds an unchanged whole-program PolyBench/C GEMM through Cgeist, Linalg raising, semantic matching, external-library lowering, and final executable generation without weak symbols, linker substitution, or source rewriting. The focused compiler regressions pass. A native-versus-raised MINI/FP64 host probe passes all 500 printed values (`max_abs=0`, `max_rel=0`); this is a pipeline/correctness probe, not an accepted performance result.
- PASS (2026-09-08) — The x86 host can deploy and execute an already cross-compiled AArch64 smoke binary through the configured Orin runner. The private endpoint and credentials are intentionally kept out of tracked artifacts. No publication timing has yet been accepted.
- IN PROGRESS (2026-09-08) — Building a static, single-thread AArch64 OpenBLAS 0.3.31 archive on the x86 host for raised/native Orin CPU correctness and timing. The build has completed the CBLAS and optimized ARMv8 kernels and is compiling LAPACK; no compilation is occurring on Orin. The next gate is unchanged-source GEMM correctness between native AArch64 and the raised OpenBLAS executable on Orin.
- PASS (2026-09-08) — Cross-compiled the unchanged PolyBench/C GEMM MINI/FP64 native executable with AArch64 GCC 11 on the x86 host and ran it successfully on Orin. It emitted the complete 500-value `C` dump and exited zero. This establishes the native side of the first silicon correctness gate; the MINI run is not a publication timing result.
- BLOCKED (2026-09-08) — The first raised AArch64 CPU link found that the interrupted OpenBLAS archive contained `cblas_dgemm` and `dpotrf_` but omitted its thread-server support members (`blas_thread_init`, `blas_thread_shutdown_`, and `blas_server_avail`). No fallback implementation or fabricated result is being used. A BLAS-only archive completion is in progress; the missing support objects are verified as real AArch64 OpenBLAS objects.
- FIXED (2026-09-08) — `compare_polybench_dumps.py` previously parsed unrelated numeric tokens from a Jetson runner banner even when explicit `DUMP_ARRAYS` markers were present, producing a false failure (`528` values instead of `500`). Marker-bearing logs are now restricted to the complete dump region; marker-free inputs retain the existing fallback. The focused regression, including arbitrary numeric banner text, passes.
- PASS (2026-09-08) — Cross-built the whole-program raised GEMM CUDA executable on x86 and ran it on Orin through real cuBLAS/CUDA. It passes the unchanged native AArch64 output for all 500 MINI/FP64 values exactly (`max_abs=0`, `max_rel=0`). This validates the raised GPU silicon path without symbol substitution; it is a correctness probe, not an accepted publication timing.
- PASS (2026-09-08) — Added and validated an algorithm-neutral in-process PolyBench repetition driver. The unchanged benchmark entry performs its own allocation, initialization, timed kernel region, and cleanup on every invocation; one process labels five warmups followed by five samples. The runner can apply an explicit execution prefix, allowing Orin CPU pinning plus `OPENBLAS_NUM_THREADS=1` and `OMP_NUM_THREADS=1`. Focused driver and runner syntax/dry-run checks pass.
- FIXED (2026-09-08) — Whole-program timing exposed an invalid Cgeist LLVM global type for libc streams: `stderr` was emitted as a memref-valued `llvm.mlir.global`. LLVM globals now preserve the translated pointer ABI. The focused `fprintf(stderr, ...)` regression passes, and whole-program standard-ABI lowering no longer requires a generated duplicate kernel wrapper.
- PASS (2026-09-08) — The first publication-size Orin correctness gate is complete for GEMM at LARGE/FP64. Native AArch64 C, raised AArch64 OpenBLAS, and raised CUDA/cuBLAS were all cross-built on x86, executed sequentially on Orin, and produced complete 1,100,000-value dumps. Both raised configurations pass the native output (`max_abs=0.01`, `max_rel=2.33819678e-05`, `rtol=5e-4`, `atol=1.1e-2`). Timing remains unaccepted until the LARGE 5+5 runs complete.
- REJECTED (2026-09-08) — The first LARGE/FP64 5+5 GEMM timing attempt produced stable 3.037292–3.044792 s samples, but an LLVM audit proved that the repeated benchmark entry executed an inlined copy of the original CPU loop instead of calling the separately transformed `kernel_gemm`. The matched cuBLAS function was present but dead. These samples are retained only as invalidation evidence and are not publication GPU data. The same audit also invalidates the preceding whole-program raised-GPU correctness claim as evidence that cuBLAS executed; correctness must be rerun after generic call preservation is fixed.
- DEFERRED (2026-09-08) — Per user direction, the whole-program GEMM call-preservation repair is not being pursued in this sweep. Measurement continues only with already audited raised/native GPU execution paths; GEMM remains explicit and unaccepted rather than being silently filled from the rejected run.
- PARTIAL (2026-09-08) — ATAX native PolyBenchGPU CUDA was cross-built on x86 and rerun on Orin. Complete-output correctness passes exactly for all 2,100 LARGE/FP64 values. One process produced five warmups plus five samples: device median 2.388704062 ms (2.385792017–2.391999960, IQR 0.003583908) and end-to-end median 8.836 ms (8.818–8.913, IQR 0.0765). Artifacts are saved under `logs/publication_orin/atax/`. Because the runner could not report the fixed power/clock/fan state (`accelerator status: N/A`), the values are retained but remain publication-pending; the raised ATAX side is next.
- PARTIAL (2026-09-08) — ATAX raised GPU also passes all 2,100 values exactly using compiler-generated real-cuBLAS DGEMV and transposed-DGEMV calls. Its one-process 5+5 medians are 0.594080 ms compute-device, 0.586560 ms synchronized resident compute-wall, 25.258208 ms compiler-visible memory-device, and 26.394144 ms end-to-end. The device/device speedup over native PolyBenchGPU is 4.0208x. Both sides are fully retained under `logs/publication_orin/atax/`; publication acceptance remains pending only because the runner reported no fixed power/clock/fan state.
- PARTIAL (2026-09-08) — BICG native PolyBenchGPU CUDA passes 4,000/4,000 LARGE/FP64 values (`max_abs=0.01`, `max_rel=2.53890878e-05`). Its one-process 5+5 medians are 2.218847990 ms device and 8.872 ms end-to-end. This native half is saved under `logs/publication_orin/bicg/`; fixed hardware-state verification is unavailable and the raised half remains to run.
- PARTIAL (2026-09-08) — BICG raised GPU also passes all 4,000 values using compiler-generated real-cuBLAS transposed-DGEMV plus DGEMV calls. One-process 5+5 medians are 0.874336 ms compute-device, 0.584928 ms synchronized compute-wall, 22.965504 ms memory-device, and 23.978720 ms end-to-end; device/device speedup is 2.537752x. The wide 0.576736–0.926144 ms compute-device range is retained explicitly. Both halves are saved under `logs/publication_orin/bicg/` and remain publication-pending because fixed hardware state was not reported.
- PASS (2026-09-08) — The fresh 2mm native PolyBenchGPU correctness executable passes all 960,000 canonical LARGE/FP64 values exactly on Orin.
- REJECTED (2026-09-08) — The first repeated 2mm native timing executable produced only 4.922–5.105 s CPU-loop timings and no `POLYBENCH_NATIVE_GPU_TIMING` records. The source compiler had cloned the non-static kernel before the strong upstream-CUDA adapter was linked. All ten samples are rejected; the timing executable is being rebuilt with the established no-IPA guards and must pass a CUDA-record/call-site audit before use.
- PARTIAL (2026-09-08) — 2mm native and raised GPU are complete and saved under `logs/publication_orin/2mm/`. Both pass complete 960,000-value LARGE/FP64 correctness. One-process 5+5 medians are native 71.743362427 ms device / 79.172 ms E2E and raised 94.298943 ms compute-device / 94.289536 ms synchronized compute-wall / 30.307904 ms memory-device / 125.136384 ms E2E. Device/device ratio is 0.760807x. Values remain publication-pending because fixed hardware state was not reported.
- PARTIAL (2026-09-08) — 3mm native and raised GPU are complete and saved under `logs/publication_orin/3mm/`. Both pass all 880,000 LARGE/FP64 values exactly. One-process 5+5 medians are native 82.369789124 ms device / 91.185 ms E2E and raised 182.088959 ms compute-device / 182.080288 ms synchronized compute-wall / 44.718400 ms memory-device / 227.371648 ms E2E. Device/device ratio is 0.452361x. Values remain publication-pending because fixed hardware state was not reported.
- UNAVAILABLE (2026-09-08) — Correlation's retained `polybenchgpu_adapters/correlation.cu` is not a normalization-only wrapper: it defines four project-authored computational CUDA kernels (`pbgpu_corr_mean`, `pbgpu_corr_std`, `pbgpu_corr_reduce`, and `pbgpu_corr_matrix`). It is therefore ineligible as a native paper baseline under the external-library-only rule. No fresh native timing is run or fabricated from this adapter; a compliant upstream/external implementation is required.
- PARTIAL (2026-09-08) — Correlation raised GPU passes all 1,440,000 values exactly through the real external-library composition. One-process 5+5 medians are 315.176270 ms compute-device, 315.164928 ms synchronized compute-wall, 19.866400 ms memory-device, and 335.643712 ms E2E. Results are saved under `logs/publication_orin/correlation/`; no native ratio is claimed, and hardware-state verification remains unavailable.
- UNAVAILABLE (2026-09-08) — Covariance's retained native adapter defines three project-authored computational CUDA kernels, so it is ineligible as a native paper baseline. No fresh native timing is run or fabricated; only the compliant raised external-library path proceeds.
- PARTIAL (2026-09-08) — Covariance raised GPU passes all 1,440,000 values through the real external-library composition. One-process 5+5 medians are 128.429794 ms compute-device, 128.419264 ms synchronized compute-wall, 20.345312 ms memory-device, and 149.188416 ms E2E. Results are saved under `logs/publication_orin/covariance/`; no native ratio is claimed and hardware-state verification remains unavailable.
- UNAVAILABLE (2026-09-08) — Doitgen's retained native adapter defines two project-authored computational CUDA kernels, so it is ineligible as a native paper baseline. No fresh native timing is run or fabricated; only the compliant raised external-library path proceeds.
- BLOCKED ATTEMPT (2026-09-08) — Reapplying the generic residency pipeline directly to Doitgen's tensor-carried matched IR failed at one-shot bufferization (`affine.for ... iter_args` was not bufferized). No timing was produced. The campaign will use the already retained correctness-approved coalesced/resident IR generated by the same automatic match, linked to the fresh ABI-only 5+5 harness.
- PARTIAL (2026-09-08) — Doitgen raised GPU passes all 3,360,000 canonical LARGE/FP64 values (`max_abs=0.01`, `max_rel=0.000337495781`). A fresh ABI-only one-process harness was cross-compiled and linked on x86 to the retained correctness-approved compiler-generated coalesced/resident GPU object; five warmups plus five samples on Orin give medians of 822.029358 ms compute-device, 822.019584 ms synchronized compute-wall, 12.219136 ms memory-device, and 834.724256 ms end-to-end. Hashes, samples, build evidence, correctness report, and IR are saved under `logs/publication_orin/doitgen/`. Native remains explicitly unavailable because its adapter contains project-authored compute kernels, and the raised timing remains publication-pending because fixed hardware state was not reported.
- REJECTED (2026-09-08) — The first fresh native Gemver correctness executable ran the upstream CUDA kernels but emitted an empty output dump. The source harness had been compiled with a visible CPU definition, and GCC's interprocedural register allocation reused the CUDA callee's return register as the later print bound. No correctness or timing result from that attempt is accepted. Adding the generic `-fno-ipa-ra` interposition guard restores an explicit `n=2000` after the external call; the corrected relocation and machine code were audited before rerunning.
- PARTIAL (2026-09-08) — Gemver native and raised GPU are complete and saved under `logs/publication_orin/gemver/`. Native passes all 2,000 canonical LARGE/FP64 outputs exactly and has one-process 5+5 medians of 2.646879911 ms device / 13.210 ms end-to-end. Raised passes all outputs (`max_abs=18721.87`, `max_rel=7.85809757e-05`) and has medians of 15.091168 ms compute-device / 15.084064 ms synchronized compute-wall / 14.472064 ms memory-device / 30.173888 ms end-to-end. Device/device ratio is 0.175393x. The native adapter contains verbatim upstream PolyBenchGPU kernels; timing remains publication-pending because fixed hardware state was not reported.
- PARTIAL (2026-09-08) — Gesummv native and raised GPU are complete and saved under `logs/publication_orin/gesummv/`. Both pass all 1,300 canonical LARGE/FP64 values exactly. One-process 5+5 medians are native 0.883328021 ms device / 6.792 ms end-to-end and raised 0.445472 ms compute-device / 0.437952 ms synchronized compute-wall / 12.289088 ms memory-device / 13.283392 ms end-to-end. The correctness-gated device/device speedup is 1.982904x. The native adapter contains the verbatim upstream PolyBenchGPU kernel; timing remains publication-pending because fixed hardware state was not reported.
- PARTIAL (2026-09-08) — MVT native and raised GPU are complete and saved under `logs/publication_orin/mvt/`. Native and raised pass all 4,000 canonical LARGE/FP64 values. One-process 5+5 medians are native 2.137023926 ms device / 8.907 ms end-to-end and raised 0.977696 ms compute-device / 0.793152 ms synchronized compute-wall / 13.800864 ms memory-device / 15.361344 ms end-to-end. Device/device speedup is 2.185775x. The native wrapper includes the upstream PolyBenchGPU source; the raised path uses real cuBLAS. Timing remains publication-pending because fixed hardware state was not reported.
- PARTIAL (2026-09-08) — SYRK native and raised GPU are complete and saved under `logs/publication_orin/syrk/`. Both pass all 1,440,000 canonical LARGE/FP64 values exactly. One-process 5+5 medians are native 83.418144226 ms device / 90.033 ms end-to-end and raised 22.748960 ms compute-device / 10.520000 ms memory-device / 33.770720 ms end-to-end. Device/device speedup is 3.666899x. The native full-matrix implementation and raised DSYRK path satisfy the same canonical lower-triangle contract; timing remains publication-pending because fixed hardware state was not reported.
- PARTIAL (2026-09-08) — SYR2K native and raised GPU are complete and saved under `logs/publication_orin/syr2k/`. Both pass all 1,440,000 canonical LARGE/FP64 values. One-process 5+5 medians are native 207.753952026 ms device / 216.215 ms end-to-end and raised 41.871967 ms compute-device / 13.884128 ms memory-device / 56.270304 ms end-to-end. Device/device speedup is 4.961648x. The native full-matrix implementation and raised DSYR2K path satisfy the same canonical lower-triangle contract; timing remains publication-pending because fixed hardware state was not reported.
- PARTIAL (2026-09-08) — FDTD-2D native PolyBenchGPU passes all 1,200,000 canonical LARGE/FP64 values and has one-process 5+5 medians of 261.527954102 ms device / 271.517 ms end-to-end. Results are saved under `logs/publication_orin/fdtd-2d/`. The raised GPU counterpart remains explicitly unavailable because there is no eligible external-library computational match for the stencil sequence; timing remains publication-pending because fixed hardware state was not reported.
- PARTIAL (2026-09-08) — Trisolv raised GPU passes the canonical LARGE/FP64 output exactly through real cuBLAS DTRSV. One-process 5+5 medians are 0.740096 ms compute-device / 0.730784 ms compute-wall / 25.085408 ms memory-device / 26.387872 ms end-to-end. Results are saved under `logs/publication_orin/trisolv/`; native PolyBenchGPU is unavailable and fixed hardware state was not reported.
- PARTIAL (2026-09-08) — SYMM raised GPU passes all 1,200,000 canonical LARGE/FP64 values through the real-cuBLAS DSYMV composition. One-process 5+5 medians are 96.199745 ms compute-device / 96.189088 ms compute-wall / 22.040512 ms memory-device / 118.612480 ms end-to-end. Results are saved under `logs/publication_orin/symm/`; native PolyBenchGPU is unavailable and fixed hardware state was not reported.
- PARTIAL (2026-09-08) — TRMM raised GPU passes all 1,200,000 canonical LARGE/FP64 values through the real-cuBLAS DTRMV/DSCAL composition. One-process 5+5 medians are 236.725891 ms compute-device / 236.716352 ms compute-wall / 14.514624 ms memory-device / 251.838464 ms end-to-end. Results are saved under `logs/publication_orin/trmm/`; native PolyBenchGPU is unavailable and fixed hardware state was not reported.
- PARTIAL (2026-09-08) — Cholesky raised GPU passes the complete 2,001,000-value canonical LARGE/FP64 lower triangle (`max_abs=1.921796055626146e-13`, `max_rel=1.921796055626146e-13`) through the validated blocked external-library composition: cuSOLVER Xpotrf panels, cuBLAS DTRSV panel solves, and cuBLAS DGEMV trailing updates. A fresh one-process 5+5 run gives medians of 520.483276 ms compute-device, 525.572640 ms for the host-side external-library region, and 530.254496 ms end-to-end. Results are saved under `logs/publication_orin/cholesky/`; native PolyBenchGPU is unavailable and timing remains publication-pending because fixed hardware state was not reported.
- PARTIAL (2026-09-08) — The publication Orin CPU campaign is validated on native ATAX at canonical LARGE/FP64. The x86-host-cross-compiled AArch64 GCC 11 `-O3` executable passes all 2,100 outputs exactly, and disassembly proves the repeated benchmark entry calls `kernel_atax`. One process pinned to Orin CPU core 0 produced five warmups plus five samples with a 6.709 ms median (6.687–6.779 ms, IQR 0.032 ms). Artifacts are saved under `logs/publication_orin_cpu/atax/`; raised residual/OpenBLAS measurements remain pending, and the timing is publication-pending because fixed hardware state was not reported.
- BLOCKED ATTEMPT (2026-09-08) — The primary AArch64 OpenBLAS 0.3.31 archive still lacks its thread-server members and initially failed to link ATAX (`blas_thread_init`, `blas_thread_shutdown_`, and `blas_server_avail`). The missing symbols are supplied by real OpenBLAS `blas_server.o` in the retained companion archive; linking both archives resolves the genuine library without adding a fallback implementation. The failed and successful link evidence is retained in the ATAX publication CPU build logs.
- PARTIAL (2026-09-08) — ATAX now has all three Orin CPU paths correctness-gated and timed. Native, compiler-generated residual, and compiler-generated real-OpenBLAS configurations pass all 2,100 canonical LARGE/FP64 values exactly. Pinned one-process 5+5 medians are 6.709 ms native, 6.081280 ms residual (1.1032x), and 4.856864 ms OpenBLAS (1.3813x). Artifacts are saved under `logs/publication_orin_cpu/atax/`; fixed hardware-state reporting remains unavailable, so the values are publication-pending.
- PARTIAL (2026-09-08) — BICG now has all three Orin CPU paths correctness-gated and timed. Native, compiler-generated residual, and compiler-generated real-OpenBLAS configurations pass all 4,000 canonical LARGE/FP64 values. Pinned one-process 5+5 medians are 18.101 ms native, 6.131936 ms residual (2.951923x), and 4.870880 ms OpenBLAS (3.716166x). Artifacts are saved under `logs/publication_orin_cpu/bicg/`; fixed hardware-state reporting remains unavailable, so the values are publication-pending.
- PARTIAL (2026-09-08) — GESUMMV native and raised real-OpenBLAS CPU paths pass all 1,300 canonical LARGE/FP64 values exactly on Orin. Pinned one-process 5+5 medians are 7.730 ms native and 2.006464 ms raised OpenBLAS, a correctness-gated 3.852549x speedup. Artifacts are saved under `logs/publication_orin_cpu/gesummv/`; fixed hardware-state reporting remains unavailable, so the values are publication-pending.
- PARTIAL (2026-09-08) — GEMVER, MVT, SYRK, SYR2K, and SYMM native and raised real-OpenBLAS CPU paths were cross-compiled on x86, correctness-gated over their complete canonical LARGE/FP64 outputs, and timed on Orin in pinned one-process 5+5 runs. Native / raised OpenBLAS medians are GEMVER 26.419 / 9.937888 ms (2.658412x), MVT 22.605 / 4.794944 ms (4.714341x), SYRK 890.491 / 91.218816 ms (9.762142x), SYR2K 3017.808 / 181.568704 ms (16.620750x), and SYMM 3665.165 / 148.566368 ms (24.670220x). Raw silicon logs, build and symbol audits, correctness comparisons, executable hashes, harness sources, and normalized sample CSVs are saved under `logs/publication_orin_cpu/{gemver,mvt,syrk,syr2k,symm}/`. Fixed hardware-state reporting remains unavailable, so all values are publication-pending.
- PARTIAL (2026-09-08) — GEMM, Doitgen, Cholesky, Trisolv, and TRMM native and raised real-OpenBLAS CPU paths are complete at canonical LARGE/FP64. All newly built paths were cross-compiled on x86 and pass complete-output correctness on Orin before timing. Native / raised OpenBLAS medians are GEMM 625.646 / 162.920224 ms (3.840199x), Doitgen 583.210 / 201.453728 ms (2.895007x), Cholesky 1614.335 / 173.534496 ms (9.302675x), Trisolv 2.814 / 2.218464 ms (1.268445x), and TRMM 2465.386 / 74.920224 ms (32.906816x). Each accepted value is from one pinned process with five warmups plus five samples and one OpenBLAS thread. The first GEMM/Doitgen/Trisolv/TRMM native timing attempt linked the PolyBench utility translation unit without `POLYBENCH_TIME`; its zero outputs were rejected, its logs retained, and corrected binaries were rebuilt and rerun. Artifacts are saved under `logs/publication_orin_cpu/{gemm,doitgen,cholesky,trisolv,trmm}/`. Fixed hardware-state reporting remains unavailable, so the values are publication-pending.
- PARTIAL (2026-09-08) — Fresh native PolyBenchGPU GEMM, correlation, and covariance were cross-compiled on x86 for `sm_87`, complete-output correctness-gated on Orin, and timed in one-process 5+5 runs. GEMM passes 1,100,000 values and has medians of 74.829246521 ms device / 81.750000 ms E2E. Correlation passes 1,440,000 values exactly and has medians of 321.237823486 / 326.549000 ms. Covariance passes 1,440,000 values (`max_abs=0.01`, `max_rel=2.91341336e-05`) and has medians of 333.460937500 / 338.829000 ms. These normalization adapters remain explicitly `modified_source=true`; correlation and covariance use the previously adjudicated canonical-normalization implementations rather than claiming untouched upstream sources. Raw silicon logs, exact cross-build logs, correctness comparisons, hashes, and normalized samples are saved under `logs/publication_orin/{gemm,correlation,covariance}/`. The runner again reported accelerator state as `N/A`, so values remain publication-pending.
- PASS (2026-09-08) — Published the fresh GEMM, correlation, and covariance native GPU medians and five-warmup scope to `performance_gpu.csv`; the correlation and covariance raised rows now also use their already retained publication-protocol resident measurements instead of legacy one-warmup values. Regenerated `/tmp/ir_viewer/polybench.html` and made the page prefer fresh publication-campaign correctness/sample artifacts over legacy timing links. The displayed values and fresh links were checked directly. A pre-existing missing f-string marker in the MFEM paper page was also corrected after the full-site link audit exposed its literal `{artifact}` path. Final full-site audit: 5,474 local links across 1,588 HTML pages, zero broken; the final targeted PolyBench refresh has 278 links and zero broken.
- BLOCKED ATTEMPT (2026-09-08) — A generic whole-program raised-CPU repetition build was tested to remove the need for per-kernel ABI harnesses, but GESUMMV matched zero launches and then dropped the selected function before final linkage. The failed build is retained as `logs/publication_orin_cpu/gesummv/whole-raised-openblas-timing-build.log`. It produced no timing and is not used; the audited dedicated-ABI harness remains the valid scalable path for raised CPU measurements.
- PARTIAL (2026-09-08) — Native Orin CPU correctness and 5+5 timing are complete for `2mm`, `3mm`, `correlation`, and `covariance`. All complete canonical LARGE/FP64 outputs pass. Pinned AArch64 GCC 11 `-O3` medians are 5260.104 ms, 5910.661 ms, 6190.619 ms, and 6405.555 ms respectively. Raw logs and samples are saved under `logs/publication_orin_cpu/<kernel>/`; raised OpenBLAS remains pending, and fixed hardware-state reporting is unavailable.
- PARTIAL (2026-09-08) — Native Cholesky passes the complete 2,001,000-value canonical LARGE/FP64 lower triangle on the Orin CPU. Its pinned one-process 5+5 AArch64 GCC 11 `-O3` median is 1614.335 ms (1609.927–1616.512 ms, IQR 3.657 ms). Canonical cubic input regeneration occurs outside the timed region and made the protocol run take 262.5 seconds; no iteration was omitted. Artifacts are saved under `logs/publication_orin_cpu/cholesky/`; raised OpenBLAS remains pending and hardware-state reporting is unavailable.
- PARTIAL (2026-09-08) — Raised real-OpenBLAS Orin CPU correctness and 5+5 timing are complete for `2mm`, `3mm`, `correlation`, and `covariance`. All complete canonical LARGE/FP64 outputs pass. Pinned single-thread medians are 204.695040 ms, 334.162688 ms, 263.700384 ms, and 249.543520 ms, yielding native-to-raised speedups of 25.697271x, 17.687974x, 23.475957x, and 25.669090x respectively. Raw logs and samples are saved under `logs/publication_orin_cpu/<kernel>/`; fixed hardware-state reporting remains unavailable.
